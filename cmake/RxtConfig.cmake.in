# CMake Package configuration file

include(CMakeFindDependencyMacro)

get_filename_component(SELF_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

# TODO - components
# foreach(component ${Rxt_FIND_COMPONENTS})
#   # For requested component, execute its "config" script
#   include(${CMAKE_CURRENT_LIST_DIR}/${component}Config.cmake)
# endforeach()
list(APPEND CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH})

# Base
find_dependency(fmt 6.0 REQUIRED)
# Data
find_dependency(Boost 1.68 REQUIRED COMPONENTS graph)
# Graphics
find_dependency(GLEW REQUIRED)
find_dependency(OpenGL REQUIRED)
find_dependency(SDL2 2.0.8 REQUIRED)
find_dependency(glm REQUIRED)
# Geometry
find_dependency(CGAL REQUIRED)

@PACKAGE_INIT@

include(${SELF_DIR}/Rxt.cmake)
